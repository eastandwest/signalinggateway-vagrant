# Install Node.js using packages crafted by NodeSource
---
- name: Ensure the system can use the HTTPS transport for APT
  stat:
    path: /usr/lib/apt/methods/https
  register: apt_https_transport

- name: Install HTTPS transport for APT
  apt:
    pkg: apt-transport-https
    state: installed
  when: not apt_https_transport.stat.exists

- name: Setup NodeSource repository
  shell: curl -sL https://deb.nodesource.com/setup_{{ nodejs_version }} | sudo -E bash -

- name: Install Node.js
  apt:
    pkg:
      - nodejs
    state: installed
    update_cache: yes



# - name: update apt
#   shell: sudo apt-get update

# - name: Install node
#   shell: sudo apt-get install -y nodejs npm

# - name: clean cache
#   shell: sudo npm cache clean

# - name: npm install n
#   shell: sudo npm install n -g

# - name: n stable
#   shell: sudo n stable

# - name: ln
#   shell: sudo ln -sf /usr/local/bin/node /usr/bin/node

# - name: purge
#   shell: sudo apt-get purge -y nodejs npm

